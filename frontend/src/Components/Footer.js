import { Layout } from 'antd';
import '../App.css';

export const Footer = () => {
  const { Footer } = Layout;
  return (
    <Footer style={{ textAlign: 'center' }}>
      Thunkable ©2021 Demo Built by O.M. Miles
    </Footer>
  );
};
